import { YesNoCommentComponent } from "./yesnocomment.component";

describe('YesNoCommentComponent', () => {

  it('should component initialized', () => {
    const component = new YesNoCommentComponent();
    expect(component).toBeTruthy();
  });
});